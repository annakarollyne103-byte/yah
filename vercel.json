{
  "installCommand": "npm install -g pnpm && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/design-e-forma exec vite build --config vite.config.prod.ts",
  "outputDirectory": "dist",
  "framework": null
}
